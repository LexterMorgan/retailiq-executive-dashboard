# Full-stack development mode (default): load data from Express API
VITE_USE_STATIC=false

# Static portfolio demo mode: load data from /public/data/*.json
# VITE_USE_STATIC=true

# GitHub Pages subpath (only needed for project-site deployments)
# VITE_BASE_PATH=/retailiq-executive-dashboard/
